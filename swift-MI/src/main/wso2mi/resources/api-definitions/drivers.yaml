openapi: 3.0.1
info:
  title: drivers
  description: API Definition of drivers
  version: 1.0.0
servers:
- url: http://localhost:8290/drivers
paths:
  /:
    get:
      responses:
        default:
          description: Default response
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
          application/xml:
            schema:
              type: object
              xml:
                name: payload
        required: false
      responses:
        default:
          description: Default response
  /{driver_id}:
    get:
      parameters:
      - name: driver_id
        in: path
        required: true
        schema:
          type: string
      responses:
        default:
          description: Default response
  /available:
    get:
      responses:
        default:
          description: Default response
